import 'package:async_redirection_navigation2/stream_auth_scope.dart';
import 'package:flutter/widgets.dart';

import 'app.dart';

void main(List<String> args) => runApp(StreamAuthScope(child: App()));
